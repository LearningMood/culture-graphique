---
import HeroBandeau from '../components/HeroBandeau.astro';
const { entry, headings = [], formation } = Astro.props;
// const toc = headings.filter((h) => h.depth === 2 || h.depth === 3); // ancienne version || traité désormais + haut dans slug.astro


// ✅ DEBUG : Vérifiez les données
// console.log('📄 Entry data:', {
//   toc : headings,
//   titre: entry.data.titre,
//   baseline: entry.data.baseline,
//   headerImage: entry.data.headerImage,
//   heroColor: entry.data.heroColor
// });
---

<HeroBandeau
  titre={entry.data.titre}
  baseline={entry.data.baseline}
  bgColor={entry.data.heroColor || '#2d2846'}
  bgImage={entry.data.heroImage}
  headerImage={entry.data.headerImage}
  textColor={entry.data.heroTextColor || 'white'}
  toc={headings}
/>


<main class="page-formation">
    <slot /> <!-- contenu MD/MDX -->
</main>

