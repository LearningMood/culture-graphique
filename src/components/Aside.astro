---
export interface Props {
  type?: 'comment' | 'source' | 'chiffre';
  side?: 'left' | 'right';
}

const { type = 'comment', side = 'right' } = Astro.props;
---

<aside class={`note ${type} ${side}`}>
  <slot />
</aside>